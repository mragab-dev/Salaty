import { AppRegistry } from 'react-native';
import App from './App';

// The name 'main' is used here because the native project is configured to look for this entry point.
// This resolves the "app entry not found" error.
AppRegistry.registerComponent('main', () => App);
